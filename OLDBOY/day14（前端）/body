body:
        - 图标 , &nbsp;(空格), &gt;（大于号）, &lt;（小于号）
        - p标签，段落
        - br标签  换行
        ============小总结==========
            所有标签分为
                块级标签：div(白板)， H系列（加大加粗），P系列（段落与段落之间有间隙）
                行内标签：span（白板）
            标签之间可以嵌套
            标签存在的意思：   css操作，js操作

        - input系列
            input type='text'     - name 属性
            input type='password' - name属性   密码（加密）
            input type='submit'   - 提交按钮，表单
            input type='button'   - 按钮

            input type='radio'    - 单选框 value， name属性（name相同则互斥）
            input type='checkbox' - 复选框 value， name属性（批量获取数据） checked="checked" #默认
            input type='file'     - 上传文件 依赖form表单的一个属性 enctype="multipart/form-data"
            input type="reset"    - 重置

            <textarea>默认值</textarea> - name属性
            select标签  - name 内部option value ，提交到后台， size，multiple（多选）

        - a标签
            - 跳转
            - 锚     href='#某个标签的ID' 标签的ID不能重复

        - img
            - src
            - alt
            - title

        - 列表
            - ul    #表示点
                li
            - ol    #表示数字
                li
            - dl
                dt  #类似标题
                dd  #类似标题下的内容

        - table标签   #表格
            - thead     #表头
                tr
                    th
            - tbody     #表数据
                tr
                    td

            colspan = ‘’    合并行
            rowspan = ‘’    合并列

        - label
             <label for="username">用户名：</label>
             <input id="username" type="text" name="user" />

        - fieldset
            legend

